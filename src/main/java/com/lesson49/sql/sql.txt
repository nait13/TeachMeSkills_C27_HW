CREATE TABLE grooup
(
    id serial NOT NULL,
    title varchar(20) NOT NULL,
    room integer NOT NULL,
    CONSTRAINT grooup_pkey PRIMARY KEY (id)
)

CREATE TABLE record_book
(
    id serial NOT NULL,
    rating integer NOT NULL,
    CONSTRAINT record_book_pkey PRIMARY KEY (id)
)

CREATE TABLE student
(
    id serial NOT NULL,
    name varchar (50) NOT NULL,
    age integer NOT NULL,
    group_id integer,
    record_book_id integer,
    CONSTRAINT student_pkey PRIMARY KEY (id),
    CONSTRAINT fk_group_id FOREIGN KEY (group_id)
        REFERENCES public.grooup (id),
    CONSTRAINT fk_record_book_id FOREIGN KEY (record_book_id)
        REFERENCES public.record_book (id)
)

insert into grooup(title,room) values('C27',12),('C26',11);
insert into record_book(rating) values(80),(37),(45),(100),(56),(77),(20),(44),(20),(33);
insert into student(name,age,group_id,record_book_id)
	values
	('Pasha',29,1,2),
	('Masha',18,2,3),
	('Sasha',30,2,4),
	('Grisha',33,2,5),
	('Dasha',26,1,6),
	('Alex',22,1,7),
	('Ruslan',27,1,8),
	('Vania',20,2,9),
	('Dima',38,2,10);


select * from student;
select * from record_book;
select * from grooup;